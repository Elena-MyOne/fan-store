export enum ROUTER_PATH {
  HOME = '/',
  PRODUCT = 'product',
  CART = 'cart',
  CHECKOUT = 'cart/checkout',
  LOGIN = 'login',
  LOGOUT = 'logout',
  REGISTRATION = 'registration',
  NOTFOUND = '*',
}
